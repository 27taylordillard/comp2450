// COMP 2450 — Floor 9 starter
// hero/BSTTests.h — declares the `selftest bst` entry point. Provided.
#pragma once

namespace dungeon {

// Runs the Floor 9 BST<int> self-test (seven phases against a fixed sample
// tree). Prints one line per phase; returns true iff all phases pass.
bool runBSTSelfTest();

}  // namespace dungeon
